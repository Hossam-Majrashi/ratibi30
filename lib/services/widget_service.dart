class WidgetService {
  const WidgetService();

  Future<void> syncTodayBudget({
    required String remainingText,
    required String summaryText,
  }) async {
    // Reserved for future Android/iOS native widget integration.
    // Business logic stays here so native wiring can be added later.
  }
}
